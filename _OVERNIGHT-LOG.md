# Manhattan Park — Overnight Build Log (`feat/mp-overnight`)

Day-39 (2026-06-08), bd8 asleep. Autonomous loop: build MP's technical depth → commit local → **never push** → bd8 reviews on wake. Focus = technical (the look is ~done). One entry per cycle.

---

- **Cycle 1 — Blue-Law fix.** The word "free" (header creed + footer creed) and the reset-button hover used electric blue as a *font color* — a Blue-Law violation (blue is highlight/glow, never font color; the prior comment rationalized it as "one permitted word"). Fixed: "free" → white text + blue text-shadow glow (lines 114, 137); reset-btn hover → gold text + blue border (line 122); de-rationalized the comment. **Verified in-browser (port 8770): both render `rgb(255,255,255)` + the blue glow.** ✓
- **Cycle 2 — a11y to the Go-page standard.** Skip-link (`href=#main`) + CSS, `<main class="wrap" id="main">` wrapper, `prefers-reduced-motion` block, `:focus-visible` ring, `overflow-x:hidden` on body — mirrored from `chambers/go/`. Sonnet bee built; **verified independently** (git diff = all 5 additions present; runtime at :8770 = skip-link / `main#main` / `overflow-x:hidden` all live). Chess engine + visuals untouched. ✓
- **Cycle 3 — `/work/` door-link (the funnel).** The chambers had no link back to the consulting page — the door was invisible from inside. Added a `/work/` cross-link to Go (`.xlink` pattern, line 97) + Manhattan Park (footer `.foot-work`, gold, line 492 + CSS). Sonnet bee built; verified via git diff (gold text, Blue-Law clean — blue stays in the footer border only). The funnel now closes from inside the chambers. ✓
- **Cycle 4 — a11y-consistency sweep (3 secondary pages).** Brought references/, about/, library/ to the Go-page a11y standard: skip-link + `<main id=main>` + prefers-reduced-motion + :focus-visible (references/about); `<div role=main>` → `<main id=main>` (library). Sonnet bee built; verified via git diff (15 insert / 5 del = additions + div→main swaps; **zero content changes** — only non-a11y diff lines were the `</div>`→`</main>` closes). **gratitude/ + homepage left untouched** (sacred / visual-risk on the key page → flagged in ROADMAP for a careful pass). ✓
- **Cycle 5 — SEO metadata (`<head>`-only).** Glass Box 3 chapters: canonical + og:url trailing-slash → `.html` (matches sitemap). six-degrees/progress: added canonical + completed OG (absolute og:image, og:url, og:type, twitter:card). JSON-LD added site-wide (was **zero**): WebSite+Organization (homepage), LearningResource (3 Glass Box chapters). **Class G caught + resolved:** preview first showed 0 JSON-LD (stale browser-cache); read the artifact (valid in index.html:24-44) + cache-busted the runtime → confirmed rendering `@graph:WebSite+Organization`, valid JSON. Zero visible changes. ✓
- **Cycle 6 — security hardening (defensive, zero behavior change).** six-degrees/progress: added `safeUrl()` (https-only) guarding the GitHub `html_url` (was `esc()`-only — encoded but didn't block `javascript:`/`data:` schemes). Manhattan Park: added `escHtml()` + wrapped the localStorage `s.rank` (was raw in innerHTML). montage: `referrerpolicy="no-referrer"` on the YouTube iframe. Sonnet bee built; verified via git diff (all defensive) + MP loads with **zero console errors**. *(Deferred: the CSP `_headers` — coupled to the daily.js style-injection fix, which is part of the marc-note restructure = your call.)* ✓
- **THE MARC NOTE — RESCUED (the front door to the key node).** The CRITICAL bug: the note lived *inside* `#the-daily`, which daily.js overwrites at runtime → the note (+ now-playing + June-7 content) was wiped before any visitor (or Marc) saw it; they got the June-6 "five good people." Fix (bd8 greenlit): moved the note OUT of `#the-daily` (now between the header and the Daily, in `.marc-note`) so daily.js can never clobber it, + updated to bd8's current words ("39 days ago…"). **Verified runtime:** note renders above the Daily, daily.js still renders the Daily ("TODAY · FIVE GOOD PEOPLE"). It shows + persists now. *(Broader daily.js restructure — the two competing Dailies, the stale `DAILY[]`, the `/daily/` archive, the now-playing, the Tesla "gave it away" line — left for the together-pass; this was the surgical rescue bd8 asked for.)* ✓
